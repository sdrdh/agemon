{
  "name": "@agemon/backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "build": "bun build src/server.ts --outdir dist --target bun",
    "start": "bun run dist/server.js",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@agemon/shared": "workspace:*",
    "@hono/mcp": "^0.2.4",
    "@modelcontextprotocol/sdk": "^1.27.1",
    "hono": "^4.6.0",
    "marked": "^17.0.4",
    "simple-git": "^3.32.3",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
